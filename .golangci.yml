version: "2"

run:
  build-tags:
    - integration

linters:
  enable:
    - bodyclose
    - copyloopvar
    - dogsled
    - dupl
    - errcheck
    - errorlint
    - funlen
    - goconst
    - gocritic
    - gocyclo
    - goprintffuncname
    - gosec
    - govet
    - ineffassign
    - intrange
    - lll
    - misspell
    - modernize
    - nakedret
    - noctx
    - nolintlint
    - revive
    - staticcheck
    - testifylint
    - unconvert
    - unparam
    - unused
    - whitespace
  exclusions:
    rules:
      # gosec's G703 taint analysis is nondeterministic across runs on the
      # same tree; test helpers only write under t.TempDir().
      - path: _test\.go
        linters:
          - gosec
        text: "G703"
  settings:
    dogsled:
      max-blank-identifiers: 3
    funlen:
      lines: 120
      statements: 60
    gocyclo:
      min-complexity: 20
    lll:
      line-length: 150
    misspell:
      locale: US
      ignore-rules:
        - cancelled
    goconst:
      ignore-tests: true
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
        - opinionated
    revive:
      # Naming a rule replaces revive's default set rather than extending it,
      # which would drop the default `exported` rule. Keep the defaults on.
      enable-default-rules: true
      rules:
        - name: marshal-receiver
        - name: multiline-if-init
        - name: use-slices-concat

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/it-bens/cc-port
